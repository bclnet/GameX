// PORT-SOURCE: Core/GameX/App/ExportManager.cs
// PORT-SHA: PLACEHOLDER
// PORT-STATUS: todo (100 live LOC)
//
// Not yet ported. Depends on `Archive` and `BinaryArchive` from
// `GameX.FileSystems`, which is the next crate in the dependency order — this
// file is the one part of `GameX` core that cannot be finished before it.
//
// Its counterpart `ImportManager` is unimplemented in the C# and documented as
// such in `app/import_manager.rs`; `ExportManager` does have a real body, so it
// is a genuine port rather than a decision.
