// PORT-SOURCE: Core/GameX/Manager.cs
// PORT-SHA: f1945cd6c19e56b3
// PORT-STATUS: done
//
// NOT PORTED — the file is empty. `sed`-stripping comments and blank lines
// leaves zero lines; PORT_MAP records it as 0 live LOC.
//
// Kept as a file so the 1:1 mapping holds and `sync-check.sh` notices if it
// gains content. `FamilyManager` itself is split across `family.rs` and
// `config.rs`, matching the C#'s `partial class`.
